You are investigating a local Ktor bug based on issue #5412.

Use JetBrains MCP `get_context_capsule` before reading any full production file.
Do not open GitHub or look up the upstream PR.
Do not edit files yet.
Do not run tests yet.

Repository:
/private/tmp/prism-demo/ktor-zstd

Bug:
A Ktor client configured to decode `Content-Encoding: zstd` crashes on large compressed responses with `ClosedByteChannelException: Destination buffer is too small`. Small zstd responses work. The failure appears only when the compressed response is large enough to be split into multiple zstd frames/chunks.

- use narrow local search only to find the likely zstd decoding implementation and candidate method;
- do not read full production files before the Prism call;
- once you have a candidate file and line, call JetBrains MCP `get_context_capsule` with:
  - projectPath="/private/tmp/prism-demo/ktor-zstd"
  - filePath=<candidate production file>
  - line=<candidate method line>
  - budget=2000
- use the Prism capsule to identify the minimal production method that should change;
- find the focused test file or nearest existing test location, using narrow search only if needed;
- explain the smallest patch strategy;
- list every file you read fully, every search query you used, and every MCP call you made;
- stop before editing.
